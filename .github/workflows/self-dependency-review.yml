# This repository's own call of the shared dependency-review workflow —
# the canon self-applies before anyone else is asked to (CLAUDE.md).
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: ./.github/workflows/dependency-review.yml
    permissions:
      contents: read # checkout for the diff of manifests
