# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@bd1439418cf430371807296efd0804d05f872d1a # v1.11.0
    permissions:
      contents: read # checkout for the diff of manifests
