# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@5bcf469d5881552f658163f808f421ac28dbc7f1 # v1.14.1
    permissions:
      contents: read # checkout for the diff of manifests
