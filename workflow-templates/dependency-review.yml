# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@dcfbd8ddb3e1e5b8cc075f422cf2a9dcea061c69 # main 2026-08-10
    permissions:
      contents: read # checkout for the diff of manifests
