# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@2f885a2782ffad83b423f9ae0452ca43e0b3158a # v1.24.1
    permissions:
      contents: read # checkout for the diff of manifests
