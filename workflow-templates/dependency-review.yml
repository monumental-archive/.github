# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@c9e057944f1307362f4f12dbdfb6e10e9a2d066d # v1.11.1
    permissions:
      contents: read # checkout for the diff of manifests
