# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@719c65597a1ba5338ed7ab45e2f2ed706474c8b2 # v1.4.1
    permissions:
      contents: read # checkout for the diff of manifests
