# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@a567e3c97791dc623b5303e3d97ff75e390ee74a # v1.19.0
    permissions:
      contents: read # checkout for the diff of manifests
