# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@5fe3824237390c5dec19a107e4b6b11f0825f3e3 # v1.21.0
    permissions:
      contents: read # checkout for the diff of manifests
