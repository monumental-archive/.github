# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@075888ca8edf295fc3e66e0e19a164e66f85580a # v1.16.0
    permissions:
      contents: read # checkout for the diff of manifests
