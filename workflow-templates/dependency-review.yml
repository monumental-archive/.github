# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@45ef64ffc00abc2971918cc78906e8bbbfc93fac # v1.4.0
    permissions:
      contents: read # checkout for the diff of manifests
