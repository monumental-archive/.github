# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@29ff2329626abc5e07dd82e3e3aec92c228bb7fb # v1.23.2
    permissions:
      contents: read # checkout for the diff of manifests
