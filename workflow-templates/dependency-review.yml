# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@97f6eb0930296219b75507fd43d9d04892c4c3b9 # v1.7.1
    permissions:
      contents: read # checkout for the diff of manifests
