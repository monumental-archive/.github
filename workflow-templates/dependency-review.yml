# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@811a481e431f28ece2d0767a822b1422b4006101 # v1.3.0
    permissions:
      contents: read # checkout for the diff of manifests
