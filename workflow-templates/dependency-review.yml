# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@ea43222ab4ef60eb658bf6c383818372a76b2fe0 # v1.5.3
    permissions:
      contents: read # checkout for the diff of manifests
