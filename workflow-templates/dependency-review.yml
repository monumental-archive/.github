# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@dbb2def4aa4d5319d99b1e9b64b8d63de72706ea # v1.26.1
    permissions:
      contents: read # checkout for the diff of manifests
