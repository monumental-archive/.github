# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@88f4c1e00314e542fe54ec134433821e3f85bd25 # v1.7.0
    permissions:
      contents: read # checkout for the diff of manifests
