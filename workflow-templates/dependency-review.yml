# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@675483e76a400848a32ac8d74b8cce77c1c6c2e5 # v1.5.2
    permissions:
      contents: read # checkout for the diff of manifests
