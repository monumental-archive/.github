# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@f16ac9b15925563283c23b80cd5aaa75e03d8150 # v1.14.0
    permissions:
      contents: read # checkout for the diff of manifests
