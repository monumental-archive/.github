# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@591afc7302d93c92c4963f29f39f4fe8c0effabc # v1.3.2
    permissions:
      contents: read # checkout for the diff of manifests
