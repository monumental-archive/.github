# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@f8e9ebea9043a5176c8b93850d4ac9089052470f # v1.15.1
    permissions:
      contents: read # checkout for the diff of manifests
