# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@3599495433d1f691e3ca24374cf11a295b2fb675 # v1.16.1
    permissions:
      contents: read # checkout for the diff of manifests
