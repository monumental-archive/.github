# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@0b218cfceae04debdb39262e48cd6ba3a0818b03 # main 2026-08-10
    permissions:
      contents: read # checkout for the diff of manifests
