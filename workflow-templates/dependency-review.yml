# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@51b1f8030a63e56e82e20a26e51decaf41e75d1e # v1.2.1
    permissions:
      contents: read # checkout for the diff of manifests
