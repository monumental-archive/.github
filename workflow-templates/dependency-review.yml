# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@d52d91b84ab838d372fdcad138d67acf537f13cf # v1.12.1
    permissions:
      contents: read # checkout for the diff of manifests
