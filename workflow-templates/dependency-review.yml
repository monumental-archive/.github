# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@ba20af2824de83e3f7b7085637e009c1f7586ad0 # v1.10.1
    permissions:
      contents: read # checkout for the diff of manifests
