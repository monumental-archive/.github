# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@0b1d19fd6daad133b9e5b850489079fe12f70adf # v1.18.0
    permissions:
      contents: read # checkout for the diff of manifests
