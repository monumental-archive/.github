# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@8893ba970bbd8921bb0c42055445e636160e2bdd # main 2026-08-09
    permissions:
      contents: read # checkout for the diff of manifests
