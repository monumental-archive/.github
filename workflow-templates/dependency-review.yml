# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@c0d92584289b5e6029d3271252a03a1b586595b2 # v1.0.0
    permissions:
      contents: read # checkout for the diff of manifests
