# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@366e25a05d9fcc9b18ab2537697599fcfe55e3d4 # v1.15.0
    permissions:
      contents: read # checkout for the diff of manifests
