# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@b3173826fdb0bc9633ebaa13f09e1c6b47a86524 # v1.2.2
    permissions:
      contents: read # checkout for the diff of manifests
