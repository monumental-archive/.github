# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@50135567a7d19aee72695a5a458624daa69f21a0 # main 2026-08-10
    permissions:
      contents: read # checkout for the diff of manifests
