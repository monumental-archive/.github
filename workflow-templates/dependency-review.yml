# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@eec2e54f93db5ee704927f8abe0c5cee7f92ceca # v1.9.0
    permissions:
      contents: read # checkout for the diff of manifests
