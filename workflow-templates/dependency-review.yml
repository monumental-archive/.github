# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@9943d269fcdb888ef25d49bf03668e703813157c # v1.10.0
    permissions:
      contents: read # checkout for the diff of manifests
