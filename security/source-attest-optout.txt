# Repositories audit:source-vsa may skip without going red (#266).
#
# A repository in the org with no founded source chain is a FINDING,
# not a skip: an unactivated repo is silent by construction — the same
# defect as the canon's own releases shipping without their SBOM from
# v1.0.0 to v1.4.1 with every board green. "Deliberate and lab-first"
# survives here as a written exception with a reason, never as silence.
#
# Format: one repository name per line, a space, then the reason.
# Remove the line the day the chain is founded; a stale entry is drift
# the Monday audit cannot see, so entries carry their removal condition.
#
# (No entries: all four org repositories are activated.)
